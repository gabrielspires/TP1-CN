#ifndef TP1_DEPENDENCIES_
#define TP1_DEPENDENCIES_

#include <iostream>
#include <cstdlib>
#include <ctime>

using namespace std;

enum Operator{ SUM = '+', SUB = '-', DIV = '/', MUL = '*'};

const string operators = "+-*/";

#endif //TP1_DEPENDENCIES_